WAF Project
######

run sudo start.sh twice from src folder, otherwise segmentation fault

~/waf/src $ sudo start.sh


To read logs,
$ tail -f ~/ludapache/logs/error_log

Req and header filter in utils.c
Aapache module in mod_lud.c